import React from 'react';
import { Link } from 'react-router-dom';

const Navbar = (props) =>
{
    return (
        <navbar>
            <h1>YAY!</h1>
        </navbar>
    );
}

export default Navbar;
